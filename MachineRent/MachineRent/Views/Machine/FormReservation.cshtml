@model Reservations
@{
    ViewBag.Title = "Formularz rezerwacyjny";
    Layout = "_Layout";
}


<div class="signup-form">
    @*<form asp-action="Login" asp-controller="Account" method="post" class="shadow p-3">
        <h2>Logowanie</h2>

        <div class="form-group">
            <input type="text" class="form-control" asp-for="Login" placeholder="Login" required="required" />
            <span asp-validation-for="Login"></span>
        </div>
        <div class="form-group">
            <input type="password" class="form-control" asp-for="Password" placeholder="Hasło" required="required" />
            <span asp-validation-for="Password"></span>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary btn-lg">Zaloguj się</button>
        </div>
        @Html.ValidationSummary()
    </form>*@

    <form asp-controller="Machine" asp-action="FormReservation" method="post" class="shadow p-3">
        <h2>Rezerwacja maszyny</h2>

        <label asp-for="RentFromDate">Wpisz datę od kiedy mamy zarezerwować</label>
        <div>
            <input asp-for="RentFromDate" type="date" class="form-control" placeholder="Od kiedy" value="Od kiedy" required="required" />
            <span asp-validation-for="RentFromDate"></span>
        </div>

        <label asp-for="RentToDate">Wpisz datę do kiedy mamy wykonać rezerwację</label>
        <div>
            <input asp-for="RentToDate" type="date" class="form-control" placeholder="Do kiedy" value="Do kiedy" required="required" />
            <span asp-validation-for="RentToDate"></span>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary btn-lg">Zarezerwuj</button>
            <a class="btn btn-primary btn-lg" asp-controller="Machine" asp-action="GetAll">Anuluj</a>
        </div>
    </form>

</div>


@*<div class="container desc-div">
        <div class="row shadow p-3 mb-5 bg-white rounded">
           
            <div>
                <div>
                    <form asp-controller="Machine" asp-action="FormReservation" method="post">

                        <label asp-for="RentFromDate">Wpisz datę od kiedy mamy zarezerwować</label>
                        <div>
                            <input asp-for="RentFromDate" type="date" placeholder="Od kiedy" />
                            <span asp-validation-for="RentFromDate"></span>
                        </div>

                        <label asp-for="RentToDate">Wpisz datę do kiedy mamy wykonać rezerwację</label>
                        <div>
                            <input asp-for="RentToDate" type="date" placeholder="Do kiedy" />
                            <span asp-validation-for="RentToDate"></span>
                        </div>
                        <div>
                            <button type="submit" onclick="return alert('Maszyna została zarezerwowana poprawnie!')">Zarezerwuj</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>*@

@*<div class="desc-div">
    <form asp-controller="Machine" asp-action="FormReservation" method="post">

        <label asp-for="RentFromDate">Wpisz datę od kiedy mamy zarezerwować</label>
        <div>
            <input asp-for="RentFromDate" type="date" placeholder="Od kiedy" />
            <span asp-validation-for="RentFromDate"></span>
        </div>

        <label asp-for="RentToDate">Wpisz datę do kiedy mamy wykonać rezerwację</label>
        <div>
            <input asp-for="RentToDate" type="date" placeholder="Do kiedy" />
            <span asp-validation-for="RentToDate"></span>
        </div>
        <div>
            <button type="submit" onclick="return alert('Maszyna została zarezerwowana poprawnie!')">Zarezerwuj</button>
        </div>
    </form>
</div>*@

